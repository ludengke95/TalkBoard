# 移除背景设置和边距设置功能

## 需求分析

移除背景设置和边距设置功能，包括：

1. 删除背景设置组件和边距设置组件
2. 从设置弹窗中移除这两个设置项
3. 从设置上下文中移除相关配置
4. 从录制代码中移除背景绘制和边距逻辑，直接按录制内容导出
5. 移除相关 CSS 样式

## 实现步骤

### 步骤 1：删除组件文件

**删除文件**:

* `src/components/Settings/BackgroundSetting.jsx`

* `src/components/Settings/MarginSetting.jsx`

### 步骤 2：修改 SettingsModal.jsx

**文件**: `src/components/Settings/SettingsModal.jsx`

* 移除 `import BackgroundSetting from "./BackgroundSetting";`

* 移除 `import MarginSetting from "./MarginSetting";`

* 移除 `<BackgroundSetting />` 和 `<MarginSetting />` 组件

* 移除预览区中背景相关的样式代码

* 移除预览区中边距相关的样式代码

### 步骤 3：修改 SettingsContext.jsx

**文件**: `src/contexts/SettingsContext.jsx`

移除 `background` 和 `margin` 默认设置：

```javascript
const defaultSettings = {
  theme: 'light',
  aspectRatio: '16:9',
  // 移除 background 设置
  cornerRadius: 12,
  camera: { ... },
  microphone: { ... },
  mouseEffect: { ... },
  // 移除 margin 设置
}
```

### 步骤 4：修改 App.jsx

**文件**: `src/App.jsx`

移除录制时的背景和边距相关代码：

* 移除 `const margin = settings.margin || 0;`

* 移除 `const bgType = settings.background.type;`

* 移除 `const bgValue = settings.background.value;`

* 移除背景图加载逻辑

* 移除绘制背景的代码

* 移除边距计算逻辑

* 直接使用 selectionBox 的尺寸作为录制画布尺寸

* 直接从 (0, 0) 开始绘制录制内容

修改后的录制逻辑：

```javascript
const startRecording = useCallback(async () => {
  if (!selectionBox || !excalidrawRef.current) return;

  // 直接使用 selectionBox 的尺寸
  const totalWidth = selectionBox.width;
  const totalHeight = selectionBox.height;

  const recordCanvas = document.createElement("canvas");
  recordCanvas.width = totalWidth;
  recordCanvas.height = totalHeight;
  recordCanvasRef.current = recordCanvas;
  const ctx = recordCanvas.getContext("2d");

  // 移除背景图加载逻辑

  // 移除摄像头初始化逻辑（如果需要保留摄像头，则保留这部分）

  const captureFrame = () => {
    if (!isCapturingRef.current) return;

    // 清空画布（不绘制背景）
    ctx.clearRect(0, 0, totalWidth, totalHeight);

    // 直接获取 Excalidraw 的 canvas
    const excalidrawCanvas = document.querySelector(".excalidraw canvas:last-of-type");
    if (!excalidrawCanvas) return;

    const containerRect = excalidrawCanvas.getBoundingClientRect();
    const scaleX = excalidrawCanvas.width / containerRect.width;
    const scaleY = excalidrawCanvas.height / containerRect.height;

    const srcX = (selectionBox.x - containerRect.left) * scaleX;
    const srcY = (selectionBox.y - containerRect.top) * scaleY;
    const srcW = selectionBox.width * scaleX;
    const srcH = selectionBox.height * scaleY;

    // 直接绘制到 (0, 0)，无边距
    ctx.drawImage(
      excalidrawCanvas,
      srcX,
      srcY,
      srcW,
      srcH,
      0,
      0,
      totalWidth,
      totalHeight,
    );

    // ... 其他逻辑
  };
  // ...
}, [...]);
```

### 步骤 5：修改 App.css

**文件**: `src/App.css`

移除背景和边距相关的 CSS 样式：

* `.background-*` 相关样式

* `.margin-*` 相关样式

## 涉及文件

| 文件                                              | 操作                          |
| ----------------------------------------------- | --------------------------- |
| `src/components/Settings/BackgroundSetting.jsx` | 删除                          |
| `src/components/Settings/MarginSetting.jsx`     | 删除                          |
| `src/components/Settings/SettingsModal.jsx`     | 移除导入和使用                     |
| `src/contexts/SettingsContext.jsx`              | 移除 background 和 margin 默认设置 |
| `src/App.jsx`                                   | 移除背景绘制和边距逻辑，直接按录制内容导出       |
| `src/App.css`                                   | 移除背景和边距相关样式                 |

## 测试要点

1. 验证设置弹窗正常显示，无背景和边距设置项
2. 验证录制功能正常工作，导出的视频无边距，无背景
3. 验证其他设置功能不受影响

